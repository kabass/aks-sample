apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poc-aks-helm.fullname" . }}-test-connection"
  labels:
    {{- include "poc-aks-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poc-aks-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
