apiVersion: apps/v1
kind: Deployment
metadata:
  name: poc-aks-frontend
  labels:
    app: poc-aks
spec:
  selector:
    matchLabels:
      app: poc-aks
      tier: frontend
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: poc-aks
        tier: frontend
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - image: pocaksacr01.azurecr.io/pocaksacr01/poc-aks:1.0
        name: poc-aks-ctr
        env:
        - name: DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: poc-aks-mysql-cm
              key: host
        - name: DATASOURCE_DATABASE_NAME
          valueFrom:
            configMapKeyRef:
              name: poc-aks-mysql-cm
              key: db_name
        - name: DATASOURCE_PORT        
          valueFrom:
            configMapKeyRef:
              name: poc-aks-mysql-cm
              key: port
        - name: DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: poc-aks-mysql-cred
              key: user
        - name: DATASOURCE_PWD
          valueFrom:
            secretKeyRef:
              name: poc-aks-mysql-cred
              key: password
        ports:
        - containerPort: 8080
          name: poc-aks
        volumeMounts:
        - name: frontend-persistent-storage
          mountPath: /app/logs
      volumes:
      - name: frontend-persistent-storage
        persistentVolumeClaim:
          claimName: frontend-pv-claim
      